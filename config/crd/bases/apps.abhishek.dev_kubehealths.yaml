---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: kubehealths.apps.abhishek.dev
spec:
  group: apps.abhishek.dev
  names:
    kind: KubeHealth
    listKind: KubeHealthList
    plural: kubehealths
    singular: kubehealth
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: KubeHealth is the Schema for the kubehealths API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeHealthSpec defines the desired state of KubeHealth.
            properties:
              namespace:
                description: Namespace to monitor
                type: string
              restartThreshold:
                description: Restart threshold after which a pod is considered unhealthy
                format: int32
                type: integer
            required:
            - namespace
            - restartThreshold
            type: object
          status:
            description: KubeHealthStatus defines the observed state of KubeHealth.
            properties:
              clusterStatus:
                type: string
              failedPods:
                format: int32
                type: integer
              healthyPods:
                format: int32
                type: integer
              lastChecked:
                format: date-time
                type: string
              restartingPods:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
